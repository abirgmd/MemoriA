package MemorIA.entity.diagnostic;

public enum QuestionType {
    TEXT,
    IMAGE,
    AUDIO,
    DATE_CHECK,      // Vérifie la date du jour
    DAY_CHECK,       // Vérifie le jour de la semaine
    MONTH_CHECK,     // Vérifie le mois
    YEAR_CHECK,      // Vérifie l'année
    SEASON_CHECK     // Vérifie la saison
}
