apiVersion: v1
kind: Service
metadata:
  name: memoria-service
  namespace: memoria-prod
spec:
  type: NodePort
  selector:
    app: memoria
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30080
